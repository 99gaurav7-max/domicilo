name: Deploy DB Schema
on:
  push:
    branches: [main]
    paths: ['database/schema.sql']
jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: psql "${{ secrets.DATABASE_URL }}" -f database/schema.sql
